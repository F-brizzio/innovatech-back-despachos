name: CI/CD Back-Despachos

on:
  push:
    branches:
      - deploy

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3

      - name: Login en Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Construir imagen Docker
        run: docker build -t lucianodelgado/innovatech-back-despachos:latest ./Springboot-API-REST-DESPACHO

      - name: Publicar imagen en Docker Hub
        run: docker push lucianodelgado/innovatech-back-despachos:latest

      - name: Desplegar en EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_BACKEND_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            docker pull lucianodelgado/innovatech-back-despachos:latest
            docker stop innovatech-back-despachos || true
            docker rm innovatech-back-despachos || true
            docker run -d \
              --name innovatech-back-despachos \
              -p 8080:8081 \
              -e DB_ENDPOINT=${{ secrets.DB_ENDPOINT }} \
              -e DB_PORT=3306 \
              -e DB_NAME=${{ secrets.DB_NAME }} \
              -e DB_USERNAME=${{ secrets.DB_USERNAME }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
              lucianodelgado/innovatech-back-despachos:latest